import React, { Component } from 'react';

class EndMessage extends Component {
    render() {
        return (
            <div>
                <label id="endMessage">That's all folks!</label>
            </div>
        );
    }
}

export default EndMessage